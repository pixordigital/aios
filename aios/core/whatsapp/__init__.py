"""AIOS WhatsApp Gateway — wrapper sobre Evolution API (engine intocada).

Arquitetura: Evolution API (Node) = engine (Baileys + Cloud API)
             Python Gateway = control plane (anti-ban, LGPD, proxy, voice, migration)
"""
__version__ = "0.1.0"
