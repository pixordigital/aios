[Unit]
Description=Cloudflare Tunnel for AIOS
After=network.target

[Service]
Type=simple
User=root
ExecStart=/usr/local/bin/cloudflared tunnel --url http://127.0.0.1:8777
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
