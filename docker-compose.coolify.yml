# AIOS — Coolify Docker Compose (Raw Compose Deployment)
# Services defined here: app + worker + voice services
# PostgreSQL & Redis are added via Coolify panel (not in compose)
# Docs: COOLIFY_SETUP.md | PRODUCTION_READINESS_REPORT.md

services:
  app:
    build: .
    ports:
      - "8777:8777"
    environment:
      AIOS_DEBUG: "false"
      AIOS_APP_URL: ${AIOS_APP_URL:-https://aios.seudominio.com}
      AIOS_DATABASE_URL: ${AIOS_DATABASE_URL:-postgresql+asyncpg://aios:CHANGE_ME@postgres:5432/aios}
      AIOS_REDIS_URL: ${AIOS_REDIS_URL:-redis://:CHANGE_ME@redis:6379/0}
      AIOS_JWT_SECRET: ${AIOS_JWT_SECRET:-CHANGE_ME_IN_PRODUCTION}
      AIOS_ADMIN_MASTER_KEY: ${AIOS_ADMIN_MASTER_KEY:-}
      AIOS_OPENROUTER_API_KEY: ${AIOS_OPENROUTER_API_KEY:-}
      AIOS_OPENAI_API_KEY: ${AIOS_OPENAI_API_KEY:-}
      AIOS_ANTHROPIC_API_KEY: ${AIOS_ANTHROPIC_API_KEY:-}
      AIOS_STORAGE_BACKEND: ${AIOS_STORAGE_BACKEND:-local}
      AIOS_S3_BUCKET: ${AIOS_S3_BUCKET:-}
      AIOS_S3_REGION: ${AIOS_S3_REGION:-}
      AIOS_S3_ENDPOINT: ${AIOS_S3_ENDPOINT:-}
      AIOS_HTTPS_ONLY: "true"
      AIOS_CORS_ORIGINS: ${AIOS_CORS_ORIGINS:-}
      AIOS_LOG_FORMAT: "json"
      AIOS_DASHBOARD_ENABLED: "true"
      AIOS_RATE_LIMIT_PER_MINUTE: "60"
    volumes:
      - app-data:/data
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8777/health/live"]
      interval: 15s
      timeout: 10s
      start_period: 90s
      start_interval: 5s
      retries: 10
    restart: unless-stopped
    deploy:
      resources:
        limits: { cpus: "1", memory: 1536M }
        reservations: { cpus: "0.25", memory: 512M }

  worker:
    build: .
    command: ["python", "-m", "aios.tasks.worker"]
    environment:
      AIOS_DEBUG: "false"
      AIOS_DATABASE_URL: ${AIOS_DATABASE_URL:-postgresql+asyncpg://aios:CHANGE_ME@postgres:5432/aios}
      AIOS_REDIS_URL: ${AIOS_REDIS_URL:-redis://:CHANGE_ME@redis:6379/0}
      AIOS_JWT_SECRET: ${AIOS_JWT_SECRET:-CHANGE_ME_IN_PRODUCTION}
      AIOS_LOG_FORMAT: "json"
    volumes:
      - app-data:/data
    depends_on:
      - app
    restart: unless-stopped
    deploy:
      resources:
        limits: { cpus: "0.5", memory: 768M }
        reservations: { cpus: "0.1", memory: 256M }

  worker2:
    build: .
    command: ["python", "-m", "aios.tasks.worker"]
    environment:
      AIOS_DEBUG: "false"
      AIOS_DATABASE_URL: ${AIOS_DATABASE_URL:-postgresql+asyncpg://aios:CHANGE_ME@postgres:5432/aios}
      AIOS_REDIS_URL: ${AIOS_REDIS_URL:-redis://:CHANGE_ME@redis:6379/0}
      AIOS_JWT_SECRET: ${AIOS_JWT_SECRET:-CHANGE_ME_IN_PRODUCTION}
      AIOS_LOG_FORMAT: "json"
    volumes:
      - app-data:/data
    restart: unless-stopped
    deploy:
      resources:
        limits: { cpus: "0.5", memory: 768M }
        reservations: { cpus: "0.1", memory: 256M }

  evolution:
    profiles: ["voice"]
    image: evoapicloud/evolution-api:latest
    environment:
      SERVER_URL: ${EVOLUTION_SERVER_URL:-http://evolution:8080}
      AUTHENTICATION_TYPE: apikey
      AUTHENTICATION_API_KEY: ${EVOLUTION_API_KEY:-evolution_secret_change_me}
      AUTHENTICATION_EXPOSE_IN_FETCH_INSTANCES: "true"
      QRCODE_LIMIT: "30"
      QRCODE_COLOR: "#00e5a0"
      DATABASE_ENABLED: "true"
      DATABASE_PROVIDER: "postgresql"
      DATABASE_CONNECTION_URI: ${EVOLUTION_DATABASE_URI:-postgresql://aios:CHANGE_ME@postgres:5432/evolution_db?schema=evolution_api}
      REDIS_ENABLED: "true"
      REDIS_URI: ${EVOLUTION_REDIS_URI:-redis://:CHANGE_ME@redis:6379/6}
      REDIS_PREFIX_KEY: evolution
      CACHE_REDIS_ENABLED: "true"
      CACHE_REDIS_URI: ${EVOLUTION_REDIS_URI:-redis://:CHANGE_ME@redis:6379/6}
      CACHE_REDIS_PREFIX_KEY: evolution-cache
      CACHE_LOCAL_ENABLED: "true"
      S3_ENABLED: "false"
      LANGUAGE: pt-BR
    volumes:
      - evolution-data:/evolution/instances
      - evolution-store:/evolution/store
    depends_on:
      - app
    restart: unless-stopped
    deploy:
      resources:
        limits: { cpus: "1", memory: 1024M }
        reservations: { cpus: "0.25", memory: 256M }

  voice-tts:
    image: ghcr.io/matatonic/openedai-speech:latest
    environment:
      TTS_HOME: /data
    volumes:
      - voice-tts-data:/data
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request;urllib.request.urlopen('http://localhost:8000/docs')\" || exit 1"]
      interval: 30s
      timeout: 10s
      start_period: 60s
      retries: 5
    restart: unless-stopped
    deploy:
      resources:
        limits: { cpus: "2", memory: 2G }
        reservations: { cpus: "0.5", memory: 512M }

  voice-stt:
    profiles: ["voice"]
    image: onerahmet/openai-whisper-asr-webservice:latest
    environment:
      ASR_MODEL: small
      ASR_ENGINE: faster_whisper
    volumes:
      - voice-stt-data:/data
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request;urllib.request.urlopen('http://localhost:9000/docs')\" || exit 1"]
      interval: 30s
      timeout: 10s
      start_period: 120s
      retries: 5
    restart: unless-stopped
    deploy:
      resources:
        limits: { cpus: "2", memory: 2G }
        reservations: { cpus: "0.5", memory: 512M }

  livekit:
    build:
      context: ./deploy/voice-stream
      dockerfile: Dockerfile.livekit
    ports:
      - "7880:7880"
      - "7881:7881"
      - "7882:7882/udp"
    environment:
      LIVEKIT_SECRET: ${AIOS_LIVEKIT_API_SECRET:-change-me-livekit-secret}
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:7880/ || exit 1"]
      interval: 15s
      timeout: 10s
      start_period: 15s
      retries: 5
    restart: unless-stopped
    deploy:
      resources:
        limits: { cpus: "2", memory: 2G }
        reservations: { cpus: "0.5", memory: 1G }

  voice-tts-stream:
    build:
      context: ./deploy/voice-stream
      dockerfile: Dockerfile.tts
    command: ["sh", "/app/entrypoint-tts.sh"]
    environment:
      VOICE_TTS_ENGINE: ${VOICE_TTS_ENGINE:-xtts}
      HF_HOME: /data/hf
    volumes:
      - voice-stream-data:/data
      - ./deploy/voice-stream/speakers:/speakers:ro
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8001/v1/voices || exit 1"]
      interval: 30s
      timeout: 10s
      start_period: 180s
      retries: 5
    restart: unless-stopped
    deploy:
      resources:
        limits: { cpus: "4", memory: 6G }
        reservations: { cpus: "1", memory: 2G }

  voice-tts-kokoro:
    profiles: ["voice"]
    image: ghcr.io/remsky/kokoro-fastapi-cpu:latest
    environment:
      USE_GPU: "false"
    volumes:
      - voice-kokoro-data:/app/api/src/models
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8880/docs || exit 1"]
      interval: 30s
      timeout: 10s
      start_period: 120s
      retries: 5
    restart: unless-stopped
    deploy:
      resources:
        limits: { cpus: "2", memory: 2G }
        reservations: { cpus: "0.5", memory: 512M }

  ollama:
    profiles: ["voice"]
    image: ollama/ollama:latest
    command: ["sh", "-c", "ollama serve & sleep 5; ollama pull ${OLLAMA_MODEL:-qwen3:8b}; wait"]
    volumes:
      - ollama-data:/root/.ollama
    restart: unless-stopped
    deploy:
      resources:
        limits: { cpus: "4", memory: 8G }
        reservations: { cpus: "1", memory: 2G }

  voice-agent:
    profiles: ["voice"]
    build: ./deploy/voice-stream
    environment:
      LIVEKIT_URL: ws://livekit:7880
      LIVEKIT_API_KEY: ${AIOS_LIVEKIT_API_KEY:-devkey}
      LIVEKIT_API_SECRET: ${AIOS_LIVEKIT_API_SECRET:-change-me-livekit-secret}
      AIOS_URL: http://app:8777
      AIOS_ADMIN_MASTER_KEY: ${AIOS_ADMIN_MASTER_KEY:-}
      OLLAMA_URL: http://ollama:11434/v1
      OLLAMA_MODEL: ${OLLAMA_MODEL:-qwen3:8b}
      OPENROUTER_API_KEY: ${AIOS_OPENROUTER_API_KEY:-}
      TTS_WS_URL: ws://voice-tts-stream:8001/tts/ws
      VOICE_TTS_ENGINE: ${VOICE_TTS_ENGINE:-xtts}
      VOICE_TTS_VOICE: ${VOICE_TTS_VOICE:-ptbr}
      KOKORO_URL: http://voice-tts-kokoro:8880/v1
      PIPER_URL: http://voice-tts:8000/v1
      STT_URL: http://voice-stt:9000/v1
    depends_on:
      - livekit
    restart: unless-stopped
    deploy:
      resources:
        limits: { cpus: "2", memory: 3G }
        reservations: { cpus: "0.5", memory: 1G }

volumes:
  app-data:
  evolution-data:
  evolution-store:
  voice-tts-data:
  voice-stt-data:
  voice-stream-data:
  voice-kokoro-data:
  ollama-data: